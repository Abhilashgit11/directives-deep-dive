import { BetterDirectiveDirective } from './better-Highlight.directive';

describe('BetterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
